from src.app.mcpServers.mcpConfigServices import McpConfigServices
from configurations.config import settings
from src.app.mcpServers.mcpConfig import mcpServers

def test_load_mcp_server():

    print(mcpServers)
    print(type(mcpServers))
